# Environment variables
PORT=8000
FRONTEND_URL=http://localhost:3000

# Database (if needed in future)
# DATABASE_URL=postgresql://user:password@localhost/alfa_payment

# Zoho CRM API Configuration
# Get these from: https://api-console.zoho.com
# 1. Create a Server-based Application
# 2. Generate Client ID and Client Secret
# 3. Generate a grant token with scope: ZohoCRM.modules.ALL
# 4. Use the grant token to get a refresh token
ZOHO_CLIENT_ID=your_client_id_here
ZOHO_CLIENT_SECRET=your_client_secret_here
ZOHO_REFRESH_TOKEN=your_refresh_token_here
ZOHO_REGION=US
# Options: US, EU, IN, AU, JP, CN
ZOHO_CRM_MODULE=Contacts

# Zoho Books API Configuration
# Required for creating vendor bills and payments
# Use the same Client ID, Secret, and Refresh Token from above
# Generate refresh token with scopes: ZohoBooks.contacts.CREATE,ZohoBooks.bills.CREATE,ZohoBooks.bills.READ
# Get Organization ID from: https://books.zoho.com/app#/settings/organization
ZOHO_BOOKS_ORGANIZATION_ID=your_organization_id_here

# Rate Limiting Configuration (optional)
# Adjust these based on your Zoho CRM plan limits
# ZOHO_MAX_CALLS=10
# ZOHO_TIME_WINDOW=10
